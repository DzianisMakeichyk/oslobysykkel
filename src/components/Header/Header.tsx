import * as React from 'react';

import { HeaderTitle, HeaderStyled } from './styles';

const Header = () => {
  return <HeaderStyled>
      <HeaderTitle>
        <span>oslobysykkel</span>
        <br />
        Ardoq
      </HeaderTitle>
  </HeaderStyled>;
};

export default Header;
