import * as React from 'react';

import { BikeIlustration, LoaderWrapper } from './styles';

const Loader = () => {
  return <LoaderWrapper>
    <BikeIlustration />
  </LoaderWrapper>;
};

export default Loader;
